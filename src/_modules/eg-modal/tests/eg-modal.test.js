'use strict';

var EgModal = require('../eg-modal');

describe('EgModal View', function() {

  beforeEach(function() {
    this.egModal = new EgModal();
  });

  it('Should run a few assertions', function() {
    expect(this.egModal);
  });

});
